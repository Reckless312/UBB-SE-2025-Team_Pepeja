<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Forum.CommentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Forum"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="0,5,0,5" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!-- Comment Author Profile Pic -->
        <Border Grid.Column="0" Width="30" Height="30" Margin="0,0,10,0" 
                CornerRadius="15" Background="LightGray">
            <Image x:Name="ProfileImage" Stretch="UniformToFill"/>
        </Border>

        <!-- Comment Content -->
        <StackPanel Grid.Column="1">
            <TextBlock x:Name="UsernameTextBlock" FontWeight="SemiBold" FontSize="12"/>
            <TextBlock x:Name="BodyTextBlock" TextWrapping="Wrap"/>
            <TextBlock x:Name="TimeStampTextBlock" Opacity="0.7" FontSize="10" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- Delete Button moved to a fixed width column-->
        <Button Grid.Column="2"
                x:Name="DeleteButton"
                Content="🗑️" 
                Click="DeleteButton_Click"
                Background="Transparent"
                BorderThickness="0"
                Visibility="Collapsed"
                ToolTipService.ToolTip="Delete comment"
                VerticalAlignment="Top"
                HorizontalAlignment="Left"
                FontSize="12"/>
        <!--<TextBlock Grid.Column="3" x:Name="asdf" Text="testing this shit" TextWrapping="Wrap"/>-->
    </Grid>
</UserControl> 