<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FriendRequests.PrivacySettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Background="#ee4fc5" Padding="10">
        <StackPanel>
            <!-- Friend Request Settings -->
            <Border Background="#ff86e0" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Friend Request Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <ComboBox SelectedIndex="0" Margin="0,0,0,10">
                        <ComboBoxItem Content="Anyone can send me friend requests"/>
                        <ComboBoxItem Content="Friends of friends can send me requests"/>
                        <ComboBoxItem Content="No one can send me friend requests"/>
                    </ComboBox>
                </StackPanel>
            </Border>

            <!-- Profile Visibility -->
            <!--<Border Background="#ff86e0" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Profile Visibility" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <ComboBox SelectedIndex="0" Margin="0,0,0,10">
                        <ComboBoxItem Content="Public (Visible to everyone)"/>
                        <ComboBoxItem Content="Friends only"/>
                        <ComboBoxItem Content="Private (Only me)"/>
                    </ComboBox>
                </StackPanel>
            </Border>-->

            <!-- Friend List Visibility -->
            <!--<Border Background="#ff86e0" CornerRadius="5" Padding="15">
                <StackPanel>
                    <TextBlock Text="Friends List Visibility" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <ComboBox SelectedIndex="0" Margin="0,0,0,10">
                        <ComboBoxItem Content="Public (Visible to everyone)"/>
                        <ComboBoxItem Content="Friends only"/>
                        <ComboBoxItem Content="Private (Only me)"/>
                    </ComboBox>

                    <Button Content="Save Settings" Width="150" Background="#4CAF50" Foreground="White" 
                            HorizontalAlignment="Right" Margin="0,20,0,0"/>
                </StackPanel>
            </Border>-->
        </StackPanel>
    </ScrollViewer>
</Page>
